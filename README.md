# block_check
This application is built with Streamlit, and enables users to clip raster tiles on-the-fly, visualize neighboring polygons, quickly annotate each block, and export structured “decision CSV” outputs for downstream analysis.
